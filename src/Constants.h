#pragma once

const float PlayerScale = 1.f;
const float PlayerHitboxWidth = 16.f;  // Simple X size for hitbox
const float PlayerHitboxHeight = 16.f; // Simple Y size for hitbox

const bool DEBUG_MODE = true;